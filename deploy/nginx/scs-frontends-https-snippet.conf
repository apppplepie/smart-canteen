# 主机 Nginx：443 终止 TLS，反代到本机 scs-frontends（默认 127.0.0.1:9080）
# 1) certbot / 自签 等准备好证书后，在已有 server { listen 443 ssl; } 里 include 本段，
#    或把整段 server 拷进站点配置，改 server_name、ssl_certificate 路径。
# 2) .env 里 FRONTEND_HOST_PORT=9080（与 compose 一致）

# server {
#     listen 443 ssl http2;
#     server_name scs.example.com;
#     ssl_certificate     /etc/letsencrypt/live/scs.example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/scs.example.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:9080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
