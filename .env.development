# 开发环境覆盖（三端共用）
# 不写则用根目录 .env 的地址（连远程）；连本机后端时取消下面注释并注释掉根目录 .env 里对应项
# VITE_API_BASE_URL=http://localhost:8081
# VITE_CHAT_API_BASE_URL=http://localhost:8081
# VITE_BASE_URL=/api/v1

VITE_PUBLIC_PATH=/
